{
  "name": "dispatch-board",
  "version": "1.0.0",
  "private": true,
  "description": "Local standalone Dispatch Board — cold-email outreach tracker backed by SQLite.",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "nodemailer": "^9.0.3"
  }
}
